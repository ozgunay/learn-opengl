if(WITH_BASE_PROJECT)
    add_subdirectory(base-project)
endif()
if(WITH_TRIANGLE_EXAMPLE)
    add_subdirectory(triangle)
endif()
if(WITH_SHADERS_EXAMPLE)
    add_subdirectory(shaders)
endif()
if(WITH_TEXTURES_EXAMPLE)
    add_subdirectory(textures)
endif()